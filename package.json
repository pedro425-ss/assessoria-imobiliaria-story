{
  "name": "assessoria-imobiliaria-story",
  "version": "1.0.0",
  "private": true,
  "description": "Gerador de Stories para Assessoria Imobiliária",
  "scripts": {
    "postinstall": "npm --prefix backend install",
    "build": "npm --prefix backend run build",
    "start": "npm --prefix backend start"
  },
  "engines": {
    "node": ">=20"
  }
}
